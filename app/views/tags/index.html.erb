<h1>These are all out tags</h1>

<ul>
<% @tags.each do |tag| %>
	<li>
	<%= link_to tag.name, tag_path(tag), class: 'tag_title', id: "tag_#{tag.id}"  %>
	<%= link_to "Delete", tag_path(tag), method: :delete, data: {confirm: "Really delete the tag?"} %>
	</li>
<% end %>
</ul>
